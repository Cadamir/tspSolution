package BruteF;

import java.io.BufferedReader;
import java.io.File;
import java.io.FileNotFoundException;
import java.io.FileReader;
import java.util.ArrayList;
import java.util.HashMap;

public class BruteForce {
    /*
    int [][] koords = new int[281][];
    public BruteForce() throws FileNotFoundException {
        BufferedReader br = new BufferedReader(new FileReader(new File("C:\\Users\\Programmierer\\Documents\\unfinished\\bee\\src\\main\\java\\tsp")));
        Koord = new ArrayList<Integer>();
        HashMap<Integer, Integer> map = new HashMap<>();
        for (int i = 1; i <= 280; i+){
            map.put(i,i);
        }
        Koord = addKoordinate(map);
    }

    public ArrayList<Integer> addKoordinate(HashMap<Integer, Integer> rest, ArrayList<Integer> al){
        if(rest.size() == 0){
            return al;
        }
        Math.random()
    }
     */
}
